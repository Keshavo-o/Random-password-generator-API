<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Password Generator</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      min-height: 100vh;
      font-family: "Poppins", sans-serif;
      color: #fff;
    }
    .container {
      margin-top: 3rem;
      max-width: 900px;
    }
    .card-custom {
      background: #2e3b44;
      border-radius: 1rem;
      box-shadow: 0 8px 20px rgba(0,0,0,0.5);
      padding: 2rem;
    }
    .table-custom {
      background: #1f2c33;
      border-radius: 0.5rem;
      overflow: hidden;
    }
    .form-control {
      border-radius: 0.5rem;
    }
    .btn {
      border-radius: 0.5rem;
      font-weight: 500;
    }
  </style>
</head>
<body>

  <div class="container">
    <div class="card card-custom">
      <h2 style="color:aliceblue;" class="text-center mb-4">
        Welcome to the Password Generator, 
        <% if(locals.user) { %><%= user.username %><% } %>
      </h2>

      <!-- Password generation form -->
      <form action="/password" method="post" class="mb-4">
        <div class="mb-3">
          <label class="form-label">User Object ID</label>
          <input type="text" class="form-control" name="userID" value="<%= user.id %>" readonly>
        </div>
        
        <div class="mb-3">
          <label class="form-label">Password For</label>
          <input type="text" class="form-control" name="passwordfor" placeholder="Enter Password For">
        </div>
        
        <button type="submit" class="btn btn-primary w-100">Generate Random Password</button>
      </form>

      <!-- Generated Passwords Table -->
      <h4 class="mb-3">Your Saved Passwords</h4>
      <div class="table-responsive">
        <table class="table table-dark table-striped table-hover table-custom">
          <thead>
            <tr>
              <th>Service / App</th>
              <th>Generated Password</th>
            </tr>
          </thead>
          <tbody>
            <% if (all_passwords && all_passwords.length > 0) { %>
              <% for (let i = 0; i < all_passwords.length; i++) { %>
              <tr>
                <td><%= all_passwords[i].passwordfor %></td>
                <td><%= all_passwords[i].password %></td>
              </tr>
              <% } %>
            <% } else { %>
              <tr>
                <td colspan="2" class="text-center text-muted">No passwords generated yet.</td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>

    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
